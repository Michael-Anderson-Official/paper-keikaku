# 手ざわり計画表

**1週間の勉強計画を書く、紙の計画表。** 教材（科目）ごとに列を作り、日 × 教材のマスに「その日やること」を書き込む。書いた計画は同じ作者の [手ざわり手帳](https://github.com/Michael-Anderson-Official/paper-techo) の**今日のページの「勉強」欄に印刷**される。

- 公開URL: https://michael-anderson-official.github.io/paper-keikaku/
- 手ざわり手帳とセットの姉妹アプリ。手帳が「日々の顔」、計画表が「1週間の設計図」。

## コンセプト

手ざわり手帳が「予定はカレンダーに、気持ちは手帳に」なら、計画表は**「計画はここに、実行は手帳で」**。

- 計画表で1週間ぶんの勉強を俯瞰して組む（教材 × 曜日のグリッド）
- その日になると、手帳の今日のページに勉強タスクが「印刷」されて届く
- 手帳側でタスクの○（できた）をつけると、計画表のマスにも赤鉛筆の○がつく（相互に反映）

紙の質感・赤いマージン線・綴じステッチ・鉛筆の○は手ざわり手帳と共通の世界観。**UI（ヘッダー・下部ツールバー・右上☰メニュー・設定カード・フォント・縁なしモバイル表示）は手ざわり手帳とほぼ同一**に揃えてある。

## ことばづかい（おとな / こども）

手ざわり手帳と同じく、画面の文言を **「大人向け（漢字）」／「やさしい（ひらがな多め）」** で切り替えられる（設定の「ことばづかい」、キーは `pk:wording`）。**既定は大人向け**。全文言を両モードで用意している（子供向け固定にはしない）。

## 連携のしくみ（2方式）

### 方式A: 同一オリジンで localStorage 共有（同じブラウザ内で使う場合）

2つのアプリは同じ GitHub Pages ドメイン（`michael-anderson-official.github.io`）配下なので localStorage を共有できる。計画表は保存のたびに**連携フィード**を書き出す:

```
pk:feed:YYYY-MM-DD = {
  v: 1,
  updated: ISO日時,
  items: [ { id, subject, text, done }, ... ]
}
```

- 本文（`text` / `subject`）の**持ち主は計画表**。手帳からは書き換えない（＝手帳では計画は「印刷されたもの」で編集不可）
- ○（`done`）は手帳からも計画表からも付け外しでき、どちらが書いても相手に反映される
- 反映のトリガー: 別タブの `storage` イベント＋アプリ復帰時（`visibilitychange` / `focus` / `pageshow`）の再描画
- **この方式はブラウザの同一オリジンが前提**。ネイティブアプリ同士（別サンドボックス）では成立しない

### 方式B: 連携コード＋即時同期（別々のアプリとして使う場合。ネイティブでも有効）

設定の「手帳と連携」で連携コードを発行/入力すると、Cloudflare Workers上の `keikakuchou-notify`（`LinkRoom` Durable Object）を介して計画データを中継する。コードを合言葉にした**サーバー経由のWebSocket同期**（`https://keikakuchou-notify.keikakuchou-app.workers.dev`への絶対URLで通信、code=query）で、押した瞬間に相手へ反映される（実測 ~1秒）。

- **ブラウザの同一オリジンに依存しない**ため、Web版どうし・ネイティブ版どうし・Web版とネイティブ版の組み合わせ、いずれでも同じしくみで繋がる。ネイティブはCapacitorのWKWebViewから直接HTTPS/WSSで通信するだけで、App Group等の追加実装は不要（見込み。実機での往路検証はMacでのビルド後に行う）
- マージは per-item の最終更新時刻によるlast-write-wins。存在集合（教材・やることの持ち主）は計画表、○は手帳・計画表どちらの変更も反映
- 連携をオフにすれば同期は止まる。保管データは400日で自動削除

### キー一覧

- `pk:subjects` = `[{id, name}]` … 教材（列）
- `pk:cell:YYYY-MM-DD:教材id` = 計画テキスト（計画表が持ち主）
- `pk:feed:YYYY-MM-DD` = 手帳へ渡す連携フィード（方式Aで使用）
- `pk:linkCode` = 方式Bの連携コード
- 週の始まりは `pk:weekStart`。無ければ手帳の `pt:weekStart` に従う（設定を共有）

## 教材をバーコードで入れる

教材を追加するとき、**本の裏の ISBN バーコードをカメラでかざすと書名を自動入力**できる（問題集・参考書など）。

- 読み取りは端末内で完結（[ZXing](https://github.com/zxing-js/library) を `vendor/` にローカル同梱・Apache-2.0）。スキャンを始めるときだけ遅延読み込みするので普段の起動は軽いまま
- 書名は **openBD（和書に強い）→ 見つからなければ Google Books** の順で引く。外部へ送るのは **ISBN だけ**（プライバシーは `privacy.html` 参照）
- **注意**: Google Books の鍵なしエンドポイントは共有の匿名クォータで、混雑時は 429 で引けないことがある（その場合は openBD 頼み／手入力にフォールバック）。洋書の取得を安定させたいなら将来 API キーの導入を検討
- カメラが使えない・読めないときは、バーコードの数字（978…）を手で入力しても引ける
- ネイティブ版はカメラ利用の説明（`NSCameraUsageDescription`）を Info.plist に設定済み

これは家族アプリで初めて「外部通信あり」の機能。使わなければ従来どおり通信は発生しない。

## 目次の写真から章を自動で入れる（OCR）

教材追加のとき「📑 目次の写真から章を入れる」で、**本の目次ページの写真（その場で撮る／写真ライブラリから選ぶ）から章を読み取って計画に入れられる**（ファイル入力に `capture` を付けていないので、iOSでは「写真ライブラリ／撮る／ファイル」を選べる）。

- ISBNから目次を返す無料APIは事実上無いため、**目次ページを撮って端末内でOCR**する方式（[Tesseract.js](https://github.com/naptha/tesseract.js) 日本語・Apache-2.0 を `vendor/tesseract/` にローカル同梱。約31MB、使うときだけ遅延読み込み）
- **OCRは完全に端末内で完結し、画像もテキストも外部に送信しない**（書名取得のISBN送信とは別物）
- 読み取った章は一覧で確認でき、○の付け外し・文字の修正ができる（OCRの誤字を直せる）
- 「今日から入れる」で、選んだ章を**今日から1日ずつ**その教材のマスへ配置（やり残しは「翌日へ繰り越す」で調整できる）

### 精度を上げる工夫

- **画像の前処理**: 撮った写真をグレースケール化＋コントラスト最大化＋適正サイズへ拡縮してからOCRへ（スマホ写真で一番効く）
- **精度重視モデル**: 日本語は `tessdata`（標準・約15MB）を使用（`fast` より誤字が少ない。例: 「オブジェクト」を正しく読む）
- **日本語＋英語（jpn+eng）**: 「TypeScript」など英字混じりの書名に対応
- **解析モード（PSM）**: 横書きは 6（一様な行）、縦書きは 5 に固定
- **縦書き対応**: 章一覧の「↕ 縦書きの本？ 縦書きで読み直す」で、同じ写真を `jpn_vert` モデルで読み直す（小説・参考書の縦組み目次向け）
- **囲み数字の正規化**: ①⑴⒈ 等をふつうの数字に直してからページ番号を除去
- それでも精度は中くらい。明るい所で目次ページをまっすぐ撮ると良い。読めないときは撮り直し／各マス手入力でも可
- **さらに上を目指すなら**: ネイティブ版で Apple Vision OCR（高精度・軽量・高速）を使う案がある（未実装。Swiftプラグイン＋Macビルドが必要）

## 教材に表紙をつける

教材の列見出しの上に本の表紙を小さく表示できる（本棚のよう）。取得方法は2通り。

**① バーコードから自動取得（ベストエフォート）**
- 「📷 本のバーコードで入れる」でスキャンすると、書名と一緒に表紙も裏で取りに行く
- **openBD**（表紙があれば端末内に縮小保存＝オフライン）→ 無ければ **Google Books**（外部URLのまま。表示のたびに読み込むのでオフライン不可）の順
- ISBNから表紙を返す安定APIは実質無い（openBDは多くが空・NDLサムネ403）ため、実際に付くのは主にGoogle Books経由。見つからなければ何もしない（②で手動設定）

**② 写真から設定（確実）**
- 「表紙を選ぶ／撮る」で表紙の写真（撮る／ライブラリから選ぶ）を設定
- 選んだ画像は幅180pxに縮小しJPEGの dataURL（1枚 数KB）で `pk:subjects` に保存。**端末内・オフライン・外部依存なし**

「はずす」で表紙を消せる。自動取得された表紙も写真で上書きできる。

## 使い方

1. 右上の「＋」で教材（英語・数学・資格の教材名など）をふやす。本なら「📷 本のバーコードで入れる」で書名を、「表紙を選ぶ／撮る」で表紙を設定できる
2. 日 × 教材のマスをタップして、その日やることを書く。「できた（○をつける）」も切り替えられる
3. 教材の見出しをタップで改名・列ごと削除・**列の並び替え**（◀▶で左右に移動。書いた計画・○は保ったまま並びだけ変わる）
4. やり残したタスクは、そのマスの「**▶ 翌日へ繰り越す**」で翌日の同じ教材へ移せる（○が付いていないマスにだけ出る。手帳側の印刷も一緒に翌日へ動く）
5. 下部ツールバーの ◀前へ／次へ▶ で週送り、「今週」で今週へ戻る
6. 右上「☰」→「設定」で ことばづかい・かみ（テーマ）・週の始まり・データのひかえ（バックアップ/復元）。「手ざわり手帳をひらく」で姉妹アプリへ

## テーマ（かみ）— 手帳と共有

アイボリー／白／黒／黒板／電光掲示板の5種類。**手ざわり手帳と共通の設定**で、選ぶと紙の色・罫線・文字色が連動する。黒板はチョーク字（Yomogi＋かすれマスク）、電光掲示板はドット字（DotGothic16＋発光）と、手帳と同じ質感まで再現。

- 計画表のテーマは `pk:theme`。未設定なら手帳の `pt:paper` に従う（週の始まりと同じ共有パターン）
- どちらのアプリで変えても、`storage` イベント／アプリ復帰時に相手側へ反映

## 技術

- 単一 `index.html`・vanilla JS・依存なし
- フォント（Klee One / Shippori Mincho）は手ざわり手帳と同じく `fonts/` に自前ホスト（Google Fontsの実配信ファイルをそのまま取得・オフライン動作。ライセンスは `fonts/OFL*.txt`）
- アイコン（`apple-touch-icon.png` / `icon-192.png` / `icon-512.png`）は紙・赤マージン線・計画グリッド・赤鉛筆の○（できたマーク）をモチーフにしたオリジナルデザイン。手帳の実写風アイコンとは絵柄が異なる（手描き風ではなく線画）
- `manifest.webmanifest` ＋ `sw.js` でオフライン対応（PWA）
- iPhone Safari 想定。スマホでは縁なし全画面

## ネイティブ化（Capacitor・手帳と同じ流儀）

App Store 配布用に Capacitor で iOS アプリとして包める（`appId: jp.tezawari.keikaku`）。中身の web 資産はそのまま `www/` にコピーするだけで、作り直しは不要。

```
npm install               # Capacitor CLI / core / ios を入れる（初回のみ）
npm run build-www         # index.html などを www/ にコピー
npx cap sync ios          # www/ を iOS プロジェクトへ反映（= npm run sync でまとめて実行）
# 以降は Mac + Xcode で ios/App を開いてビルド・提出
```

- iOS プロジェクトは `ios/`（Capacitor 8 = Swift Package Manager 構成。CocoaPods 不要）。アプリアイコンは `ios/App/App/Assets.xcassets/AppIcon.appiconset/` に 1024×1024 を設置済み
- `node_modules/`・`www/`・`ios/App/App/public/` は生成物のため git 管理外（`.gitignore`）
- **ビルド・提出は Mac（Xcode）と Apple Developer Program（年 $99）が必要**。この作業は開発者の Mac 側で行う
- **連携について**: 方式A（localStorage共有）は同一オリジンのブラウザでのみ成立するためネイティブアプリ同士では効かないが、方式B（連携コード＋Cloudflare Workers中継）はHTTPS/WSSの絶対URL通信のみでApp Group等に依存しないため、ネイティブ版どうしでも同じ連携コードで繋がる見込み。実機での動作確認はMacでの両アプリビルド後に行う

## 未実装（製品化するなら）

- テンプレート（曜日ごとの定番タスク）
- まとめて繰り越し（先週の未完了を今週へ一括で持ってくる。現状は1マスずつ手動）
- 洋書向け Google Books API キー
- スプラッシュ画面の作り込み（現状は Capacitor 既定）
