# AGENTS.md — 手ざわり計画表 開発の引き継ぎ

このファイルは、AIエージェント（Codex 等）がこのリポジトリで作業を続けるためのガイドです。
プロダクトの仕様・連携のしくみは `README.md` に詳しくあるので、ここでは**作業の約束事・落とし穴・手順・残タスク**に絞ります。

---

## このアプリは何か（1行）

司法試験予備試験を勉強中のユーザー本人が使う、**1週間の勉強計画を書く紙質感のPWA**。
姉妹アプリ [手ざわり手帳](https://github.com/Michael-Anderson-Official/paper-techo) と連携し、書いた計画が手帳の今日のページに「印刷」される。詳細は `README.md`。

- 公開URL: https://michael-anderson-official.github.io/paper-keikaku/
- ホスティング: GitHub Pages（`main` ブランチ push で自動デプロイ）

## アーキテクチャの大前提

- **単一ファイルのバニラJS**。`index.html` にHTML/CSS/JS が全部入り。**ビルド工程なし・依存ライブラリなし・フレームワークなし**。
- `sw.js` = Service Worker（オフラインキャッシュ）。`manifest.webmanifest` = PWA マニフェスト。
- 永続化は **localStorage のみ**。キー体系は `README.md` の「キー一覧」を参照。
- `ios/` `www/` `capacitor.config.json` は Capacitor ネイティブ化の残骸/準備。**通常のWeb作業では触らない**。

## 絶対に守るルール

1. **`index.html` を変更したら、必ず `sw.js` の `CACHE` バージョンを上げる。**
   `var CACHE = 'tezawari-keikaku-vNN';` の `NN` を +1。これを忘れると利用者に更新が届かない。
   （現在の最新は履歴を見て確認すること。2026-07-25 時点で v55。）

2. **データを黙って捨てない。** 過去にデータ消失事故があった。localStorage のフォーマットを変えるときは、
   旧フォーマットを読める**後方互換マイグレーション**を必ず同梱する（`readDetail()` の移行処理が実例）。

3. **セル項目オブジェクトに独自フィールドを生やさない。**
   `pk:cell:*` の項目 `{id, text, done, u}` は、保存(`cellSave`)・繰り越し・リモート同期適用(`applyRemoteFeed`)の
   たびに**固定フィールドから作り直される**。項目に勝手なフィールドを足しても次の保存/同期で消える。
   付帯情報が要るときは**別のキー名前空間**（例 `pk:detail:*`）に持つこと。

4. **勝手に「自動化」を足さない。** これはユーザーの明確な方針。
   紙のアナログ運用に近い「書いたものがそのまま出る」シンプルさを重視しており、
   システムが賢く計算する機能（自動スケジューリング等）はこれまで何度も追加→撤去されている。
   賢いロジックより単純さを選ぶ。迷ったらユーザーに聞く。

5. **どの画面/機能の話か曖昧なときは、推測せず先に確認する。**
   過去に「メイン画面」と「モーダル」を取り違えて手戻りが発生している。

## 文言（コピー）の方針

- 地の文・UIの既定は**大人向け（漢字）**。子供向けの易しい表記は切替で用意するが、子供向け固定にはしない。
- 文言は `index.html` 内の `WORDS`（`WORDS.xxx` を `tx('xxx')` で参照）に集約。
  ただし一部のボタンは静的HTMLの文字がそのまま出る（`applyWording()` が上書きしていないもの）。両方確認すること。

## 変更→デプロイの標準手順

1. `index.html`（必要なら他ファイル）を編集。
2. **自己検証**：ローカルにサーバーを立て、実ブラウザ（Playwright 等）で
   スクショ**と** `getBoundingClientRect()`/`getComputedStyle()` の**数値**の両方で確認する。
   DOM件数を数えるだけの合成チェックは、実機の見た目・重なり・カクつきを見逃すので不十分。
   - 検証の実行例は開発者のスクラッチに使い捨てスクリプトを置く運用（このリポジトリには含めない）。
   - **既知の制約**：Playwright/Chromium は `env(safe-area-inset-*)`（ノッチ/ダイナミックアイランド）を
     再現しない（常に 0px）。safe-area 関連の見た目は実機（iPhone Safari）でしか最終確認できない。
     実機は **iPhone 17 Pro（論理ビューポート 402×874）**。まず幅 402px で検証する。
3. `sw.js` の `CACHE` を +1。
4. コミット（メッセージは日本語で、何を・なぜ変えたかを具体的に）。→ `git push`。
5. GitHub Pages に反映されたか、`sw.js` を curl で数十秒ポーリングして新バージョン文字列を確認する。
6. force push 禁止。破壊的操作の前は必ず `git status`/`git diff` で中身を確認。

## レイアウトの現状メモ（ハマりどころ）

- メイン画面のヘッダー群（`.page-date` / `.exam-countdown` / `.grid-scroll`）は
  `position: absolute` で **上から px 決め打ちの積み上げ**構造。要素を足すと後続の `top` を手で直す必要があり、
  過去に safe-area の**二重加算**バグを2回出している。理想は flex 縦並びへの作り替え（未着手・大きめの作業）。
- `.exam-countdown` は高さが内容で変わるため、JSで実測して CSS変数 `--countdown-h` に入れ、
  後続要素の `top: calc(... + var(--countdown-h))` で押し下げている。ここを触るときは二重加算に注意。

## 今の残タスク（UX一新プロジェクト）

ユーザー要望「全UIを一新。もっと直感的・シンプル・操作しやすく」を、**1項目ずつ実装→検証→デプロイ**で進めている。
見た目のブランド（紙/黒板の質感）は維持し、変えるのは**操作感(UX)**。

- [x] 上部の情報量を圧縮（常時表示の案内文を撤去）— v53
- [x] 設定モーダルを整理（技術的項目を折りたたみ `<details>` に）— v54
- [x] セルの書き込み画面を洗練（○/繰り越し/削除の記号の紛らわしさを解消）— v55
- [ ] **メインの週グリッド** ← 次はここ。
      課題感：教材（列）が増えると横に窮屈、どこに何を書くか分かりにくい、等。
      具体の方向はユーザーと握ってから着手すること（勝手に列廃止などの大改造をしない）。

### 重要：一度差し戻した改修がある（2026-07-25）

v56〜v63 で、週グリッドの列切り替え・セル入力の自動保存・タップ領域44px化・
コントラスト調整・表の縦横スクロール廃止（7日固定）などを一気に入れたが、
**ユーザーの実機で「日付と教材の間に無駄な余白が増えた」という回帰が出て、
最終的に v55 の状態まで全部差し戻した**（この AGENTS.md も含む）。

- 差し戻し前の実装は git 履歴に残っている（`7aa1d29` が Codex 改修の最終状態、
  `5f1ebf1` がその後の余白修正の試み）。参照はできるが、**そのまま復活させない**。
- 教訓1：**一度に多くの変更を入れない。** 1項目ずつ実装 → ユーザーの実機確認 →
  次へ、を守る。まとめて入れると、どの変更が回帰を生んだか切り分けられなくなる。
- 教訓2：レイアウトの余白・位置の不具合は、**Playwright では再現しないことがある**
  （`env(safe-area-inset-*)` が 0px、Web フォントの読み込みが速すぎる等）。
  実機確認を挟まずに「直った」と判断しないこと。
- 教訓3：この余白の原因は最後まで確定していない。フォント読み込みタイミングだと
  推定して `ResizeObserver` で高さを再計測する修正を入れたが、**実機では直らなかった**。
  再挑戦するなら、まず実機スクショで「どの2要素の間か・何px か」を確定させること。

## やってはいけない例

- ビルドツール/npm 依存/フレームワークの導入（このアプリの思想に反する）。
- `sw.js` のバージョン据え置きでの `index.html` 変更。
- セル項目への独自フィールド追加（3項参照）。
- 「便利だろう」という自動計算・自動スケジューリングの独断追加（4項参照）。
